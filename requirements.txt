# Core runtime. A CUDA GPU is required; the Triton kernels have no CPU path.
torch>=2.1
triton>=2.1
transformers>=4.38
accelerate>=0.26
datasets>=2.16
numpy>=1.24
matplotlib>=3.7

# Optional. Enables the bitsandbytes NF4 baseline in main.py and
# main_ternary.py. Both scripts catch the ImportError and skip that
# comparison when it is absent.
# bitsandbytes>=0.42
